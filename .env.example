# Groq API Key for AI responses (Required)
GROQ_API_KEY=gsk_your_api_key_here

# Port for the web dashboard (Optional, defaults to 3000 locally, Railway injects this automatically)
# PORT=3000

# Dashboard Login Credentials (Defaults to admin / admin if not set)
DASHBOARD_USER=admin
DASHBOARD_PASS=@ANurag100
